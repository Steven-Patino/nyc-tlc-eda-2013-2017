# 🗂️ Datasets auxiliares

Carpeta reservada para datasets ligeros o derivados que no pertenezcan al flujo principal.

El flujo actual usa `raw_datasets/` como entrada y `outputs/new_datasets/` como salida.

